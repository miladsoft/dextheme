@using Microsoft.AspNetCore.Components.Web
@using Blockcore.AtomicSwaps.Theme._dexhemes;
@using Blockcore.AtomicSwaps.Theme._dexhemes.libs

@namespace Blockcore.AtomicSwaps.Theme.Pages
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@inject IBootstrapBase BootstrapBase
@inject IDEXTheme DEXTheme

@{
    BootstrapBase.init(DEXTheme);
}

<!DOCTYPE html>
<html @Html.Raw(DEXTheme.printHtmlAttributes("html"))>
<!--begin::Head-->

<head>
    <base href="~/" />
    <meta charset="utf-8"/>
    <meta name="description" content=""/>
    <meta name="keywords" content=""/>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <meta property="og:locale" content="en_US"/>
    <meta property="og:type" content="article"/>
    <meta property="og:title" content=""/>

    <link rel="shortcut icon" href="@DEXTheme.getFavicon()"/>

    <!--begin::Fonts-->
    @foreach (string font in DEXTheme.getFonts())
    {
        <link rel="stylesheet" href="@font"/>
    }
    <!--end::Fonts-->

    <!--begin::Global Stylesheets(mandatory)-->
    @foreach (string file in DEXTheme.getGlobalAssets("Css"))
    {
        <link rel="stylesheet" type="text/css" href="@file"/>
    }
    <!--end::Global Stylesheets-->
</head>
<!--end::Head-->

<!--begin::Body-->
<!body @Html.Raw(DEXTheme.printHtmlAttributes("body")) class='@DEXTheme.printHtmlClasses("body")'>
    <partial name="~/Shared/Partials/ThemeMode/_Init.cshtml"></partial>

    <!--begin::Global Javascript(mandatory)-->
    @foreach (string file in DEXTheme.getGlobalAssets("Js"))
    {
        <script src="@file"></script>
    }
    <!--end::Global Javascript-->

    <partial name="~/Shared/Partials/_PageLoader.cshtml"></partial>
    
    @RenderBody()


    
    <script src="_framework/blazor.server.js"></script>
</!body>
<!--end::Body-->

</html>
